## 🛠 Setup Instructions

1. **Create a `.env` file** in the root of your backend folder with the following values:

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=cron_manager
PORT=5000


2. **Initialize the database tables** by running:

node setup.js